# 2026-07-03 Work Log

## Summary

- Confirmed that future changes should be applied to the real project directory `E:\Bruce\Calibration`.
- Updated calibration view click marker from a red dot to a green crosshair.
- Merged `Use Center 1` and `Use Center 2` into a single `Use Center` action.
- Added chessboard diagonal grayscale filtering for `cv2.goodFeaturesToTrack` candidate corners.

## Calibration UI

- Modified `CustomGraphicsView.draw_cursor()`:
  - Removed the red ellipse marker.
  - Added a green crosshair using two `QGraphicsLineItem` objects in a `QGraphicsItemGroup`.
  - Kept `ItemIgnoresTransformations` so the marker size stays stable during zoom.
- Updated `PageCalibration.ui` and `Ui_PageCalibration.py`:
  - Replaced `btnCenterCam1` and `btnCenterCam2` with one `btnCenter`.
  - Button text changed to `Use Center`.
- Updated `PageCalibration` logic:
  - Added `use_center_seeds()`.
  - One click now records both camera center points from the latest clicked point in camera 1 and camera 2 views.
  - If either camera has no selected point, the UI prompts the user to click the missing view first.

## Corner Candidate Filtering

- Added chessboard-specific filtering after `cv2.goodFeaturesToTrack`.
- New helper `_has_chessboard_diagonal_pattern()`:
  - Samples four small patches around a candidate corner.
  - Checks that NW/SE grayscale values are similar.
  - Checks that NE/SW grayscale values are similar.
  - Checks that the two diagonal groups have enough contrast.
- Added `_patch_mean()` to calculate local grayscale patch means safely.
- Applied the same diagonal grayscale filter during manual grid expansion after `cornerSubPix` refinement.

## Config

- Added calibration parameters in `src/cnf.ini`:
  - `manual_diagonal_sample_px = [6]`
  - `manual_diagonal_max_diff = [35]`
  - `manual_diagonal_min_contrast = [20]`

## Files Changed

- `src/widgets/cutom_widgets.py`
- `src/modules/PageCalibration.ui`
- `src/modules/Ui_PageCalibration.py`
- `src/modules/page_calibration.py`
- `src/cnf.ini`

## Verification

- Ran Python compile check on the real project file:

```powershell
py -m compileall E:\Bruce\Calibration\src\modules\page_calibration.py
```

- Result: compile passed.

## Notes

- Earlier changes were made in the Codex worktree first, then synchronized to `E:\Bruce\Calibration`.
- The working convention is now: future implementation changes should be applied to the real project directory `E:\Bruce\Calibration`.
