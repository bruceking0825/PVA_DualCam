# 2026-06-30 工作日志

## 项目背景

将 `matlab_version/Calibration` 中的 MATLAB 双目相机标定程序迁移为 Python 版本，并尽量贴近 `E:\Bruce\Sorter` 项目的 PySide6/PyDracula 工程风格。

## 今日完成

1. 梳理 MATLAB 标定程序结构

   - 主流程：`calibrationb.m`
   - 单相机标定入口：`cacl.m`
   - 相机配置：`configc.m`
   - 外参初值：`extinit.m`
   - 相机模型：`cmodel.m`
   - LM 优化：`lmoptc.m`
   - 投影验证：`XYZproj.m`、`XYZproj2.m`
   - 角点辅助：`cornerfinder.m`、`igrdpeak.m`

2. 解析 `calibration_pic6.mat`

   - 确认文件格式为 `MATLAB 5.0 MAT-file`。
   - 读取到 135 个变量。
   - 关键变量：
     - `cam1dat`: `721x8`
     - `cam2dat`: `697x8`
     - `parc1/parc2`: 单相机内参
     - `posc1/posc2`: 单相机外参
     - `Ac1/Ac2`: 两台相机投影矩阵
     - `cvctr1/cvctr2`: 镜头中心世界坐标
     - `outdata`: `89x1`，对应旧版 `ccal001.csv`

3. 建立 Python 工程骨架

   - `src/calibration_core/config.py`
   - `src/calibration_core/initialization.py`
   - `src/calibration_core/legacy_model.py`
   - `src/calibration_core/legacy_calibration.py`
   - `src/calibration_core/stereo_export.py`
   - `src/calibration_core/io.py`
   - `src/run_calibration.py`
   - `src/inspect_mat.py`
   - `src/modules/page_calibration.py`
   - `src/main.py`

4. 移植核心算法

   - 移植 MATLAB `configc` 到 Python `CameraConfig`。
   - 移植 MATLAB `extinit` 为 `estimate_external_initial`。
   - 移植 MATLAB `cinit/cmodel/XYZproj/XYZproj2` 对应投影模型。
   - 使用 `scipy.optimize.least_squares(method="lm")` 实现单相机标定。
   - 实现双目标定结果导出，生成兼容旧格式的 `ccal001.csv` 输出向量。

5. 增加 Sorter 风格页面雏形

   - 新增 `PageCalibration`。
   - 支持选择 `.mat` 文件、输出 CSV 路径、相机配置和最大迭代次数。
   - 标定计算放入 `QThread`，避免阻塞 UI。

6. 修正已发现的移植问题

   - 修正 `extinit` 中 MATLAB `reshape(a,4,3)'` 的 NumPy 等价写法。
     - 错误写法：`reshape((3, 4), order="F")`
     - 正确写法：`reshape((3, 4))`
   - 同步修正平面分支 `reshape(a,3,3)'`。
   - 修正 CSV 导出中 `reshape(Ac1',12,1)`、`reshape(Ac2',12,1)`、灵敏度矩阵的展开顺序。

7. 编写说明文档

   - 新增 `README.md`。
   - 新增 `requirements.txt`。
   - 说明了如何运行 `.mat` 回归、如何接入 Sorter。

## 今日验证

1. 语法检查通过

   ```powershell
   py -m py_compile src\run_calibration.py src\inspect_mat.py src\main.py src\calibration_core\*.py src\modules\page_calibration.py
   ```

2. 当前 `py.exe` 环境缺少 `numpy`

   运行 `.mat` 读取工具时发现：

   ```text
   ModuleNotFoundError: No module named 'numpy'
   ```

   因此完整数值回归还未在当前 Python 环境跑通。

## 关键输出格式说明

`ccal001.csv` 的前 35 行含义：

| 行号 | 内容 |
| --- | --- |
| 1-5 | `sys(1:5)`，相机系统参数 |
| 6 | camera1 标定误差 `std(er1)` |
| 7-12 | `posc1`，camera1 外参 |
| 13-20 | `parc1`，camera1 内参 |
| 21-23 | `cvctr1`，camera1 镜头中心世界坐标 |
| 24-35 | `Ac1`，camera1 的 `3x4` 投影矩阵 |

## 待办事项

1. 在带有 `numpy/scipy/PySide6` 的项目环境中安装依赖并跑完整回归。

   ```powershell
   pip install -r .\requirements.txt
   python .\src\run_calibration.py --mat .\matlab_version\Calibration\calibration_pic6.mat --camera legacy_pic6 --out .\ccal001.csv
   ```

2. 将 Python 输出与 MATLAB `.mat` 中的以下变量逐项对比：

   - `parc1/parc2`
   - `posc1/posc2`
   - `Ac1/Ac2`
   - `cvctr1/cvctr2`
   - `outdata`

3. 接入 `E:\Bruce\Sorter`

   - 复制 `calibration_core`。
   - 复制 `page_calibration.py`。
   - 在 Sorter 的 `ui_functions.py` 中注册页面和按钮。

4. 后续增强

   - 增加从 `cal0.bmp` 到 `cal5.bmp` 自动读取拼接图的流程。
   - 用 OpenCV 棋盘角点检测替代 MATLAB 半手动点选。
   - 增加手动角点修正兜底。
   - 增加结果可视化：重投影误差、`Ac1/Ac2` 坐标轴投影、异常点列表。

## 风险记录

1. MATLAB 原算法使用自定义 Heikkila 模型，不能简单替换为 OpenCV `calibrateCamera`，否则输出格式和数值含义可能不兼容旧下游。
2. `extinit` 和 `reshape(Ac1',12,1)` 这类 MATLAB/NumPy 行列顺序差异很容易引入隐蔽误差，后续需要持续用 `.mat` 做回归。
3. 当前 Python 工程还没有直接写入 `E:\Bruce\Sorter`，因为当前可写工作区是 `E:\Bruce\Calibration`。
